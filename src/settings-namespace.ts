import type { SettingsNamespace } from '@deepseek-ai/dsh-settings'

/** Canonical settings namespace shared by Host and Client halves. */
export const SHORTCUTS_SETTINGS_NAMESPACE = 'dsh-ui-shortcuts' as SettingsNamespace
