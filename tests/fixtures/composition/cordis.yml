# Minimal patch overlay applied after the DSH Web profile bundle layers.
# Web runtime rows come from @deepseek-ai/dsh-web-app; this overlay only adds
# the installed shortcuts bundle row.
- insert:
    - id: dsh-ui-shortcuts
      name: '@hytime/dsh-client-ui-shortcuts'
